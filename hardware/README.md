# Hardware notes

Photos, scope captures and board revisions live here.

During bring-up, drop in:
- `donor-pcb-*.jpg` — the buzzer's board and its radio daughterboard, both sides
- `sck-capture-*.png` — logic analyser trace of the radio's SPI clock (this decides
  the ESP32 approach; see `docs/hardware.md`)
- `wiring-*.jpg` — the breadboard build as actually wired
